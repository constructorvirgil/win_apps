<Window x:Class="WinApps.MessageCenter.OverlayToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Topmost="True">
  <Border x:Name="Root"
          Background="#CC151515"
          CornerRadius="10"
          Padding="12"
          TextElement.FontFamily="Microsoft YaHei UI">
    <Border.Effect>
      <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.35" />
    </Border.Effect>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <TextBlock x:Name="TitleText"
                 Grid.Row="0"
                 Grid.Column="0"
                 Margin="0,0,8,0"
                 FontSize="14"
                 FontWeight="SemiBold"
                 Foreground="#FFFFFFFF"
                 TextTrimming="CharacterEllipsis" />

      <Button x:Name="CloseButton"
              Grid.Row="0"
              Grid.Column="1"
              Width="22"
              Height="22"
              Padding="0"
              Background="#00FFFFFF"
              BorderBrush="#33FFFFFF"
              Foreground="#FFFFFFFF"
              FontSize="14"
              FontWeight="Bold"
              Content="×"
              Focusable="False" />

      <TextBlock x:Name="MessageText"
                 Grid.Row="1"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 Margin="0,8,0,0"
                 FontSize="12"
                 Foreground="#E6FFFFFF"
                 TextWrapping="Wrap" />
    </Grid>
  </Border>
</Window>
